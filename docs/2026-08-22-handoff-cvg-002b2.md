# Handoff de continuidade — CVG-002B2 PIX durável

**Checkpoint:** 23 de agosto de 2026, após `EVT-0092` (reconciliação do DLQ
operacional e da semântica multi-réplica)

**Branch:** `agent/sync-v4-full-program`

**Último slice de implementação publicado:** `35f68fd` (`feat: add pix settlement dlq operations`), com correção de observabilidade em `1217882`, ambos pushed para `origin/agent/sync-v4-full-program`.
**Última reconciliação de ledger publicada:** `d525acc` (`docs: clarify replicated dlq review evidence`); o checkpoint documental derivado desta onda foi publicado em `76f7ec5` para `origin/agent/sync-v4-full-program`. `8d226d0`, `3c76ce0`, `409efea` e `f0c38c3` permanecem como histórico.

**Ponto de entrada curto desta sessão:** [`2026-08-23-checkpoint-continuacao.md`](2026-08-23-checkpoint-continuacao.md), com a reauditoria de 1.447 arquivos e a ordem de retomada operacional.
O checkpoint curto foi publicado em `f0c38c3` (`docs: save continuation checkpoint`); os ledgers foram reconciliados em `d525acc` e os documentos derivados desta onda foram publicados em `76f7ec5`.

**Último checkpoint de implementação:** principal de serviço, ACL/RLS e consumer PIX registrados em [`.agent/artifacts/CVG-002B2B-service-principal-worker-2026-08-22.md`](../.agent/artifacts/CVG-002B2B-service-principal-worker-2026-08-22.md)

**Programa ativo:** `CVG-002` — jornada atômica de atendimento até recebimento

**Estado honesto:** `CVG-002B1` e `CVG-002B2a` continuam verificados; parser/ingress, HTTP→PostgreSQL, migration de principal, filtros de login, ACL/RLS, UoW compartilhada, retry transitório, takeover limitado, barreira legada `410` e consumer B1 cercado estão implementados e testados como slices limitados. Nesta sessão o caminho padrão de API key PostgreSQL foi corrigido com lookup pré-contexto por capability `SECURITY DEFINER`, mapper JSONB estrito, tabelas de uso/rate-limit tenantizadas, ACL worker/API e rate limit obrigatório nas rotas extraídas; a prova HTTP real agora passa sem adapter. `CVG-002B2B` ainda não está completamente verificado: restart de processo real, DLQ operacional, política multi-réplica de rate limit, principal autenticado mínimo, B2c, provider real e produção continuam pendentes.

Este documento é o ponto de entrada para a próxima sessão. O contrato técnico executável está em [`.agent/tasks/CVG-002B2B.md`](../.agent/tasks/CVG-002B2B.md); o estado canônico continua em `.agent/`.

## 1. Estado operacional atual

- `active_task`: `CVG-002B2B`.
- `lifecycle_stage`: `BUILD`.
- `active_activity`: `TEST`.
- `status`: `IN_PROGRESS`.
- `verification_state`: `PARTIAL`.
- `next_gate`: `VERIFIED`.
- `last_event_id`: `EVT-0092`.
- O checker canônico retorna `11 PASS`, `1 WARN` histórico (`CVG-001` e `CVG-002` simultaneamente `IN_PROGRESS` sob `root integrator`) e `0 FAIL`.
- O único caminho pré-existente e user-owned que deve permanecer fora do commit é `packages/design-system/tsconfig.vue.tsbuildinfo`.
- O relatório consolidado de auditoria/pesquisa está em [`2026-08-22-auditoria-integral-e-pesquisa-erp.md`](2026-08-22-auditoria-integral-e-pesquisa-erp.md). Ele registra 0/11 paridade Vetus, os fallbacks in-memory, a lacuna de atomicidade cross-domain e o benchmark oficial atualizado de PIMS que orienta a próxima execução.

Comandos de retomada:

```bash
cd /home/ricardo/cvg-his-v4
git switch agent/sync-v4-full-program
git status --short
python3 /home/ricardo/.codex/skills/engineering-framework/scripts/check_state.py "$PWD"
```

Não reverta, remova nem inclua o cache do design system.

## 2. O que está verificado antes do B2b

### `CVG-002B1` — núcleo confirmado de settlement PIX

- Commit de implementação: `5387ebe`.
- Commit de checkpoint: `eaaeede`.
- Uma única tenant UoW cria receivable/payment, atualiza billing e PIX, grava journal balanceado, prova append-only, audit e outbox.
- Inbox provider-scoped e replay canônico impedem efeitos duplicados.
- PostgreSQL principal: 14/14, incluindo concorrência, tenant/RLS e failpoints por escrita.
- Limite: DB-only; não comprova callback, worker, provider real ou SPA.

### `CVG-002B2a` — request e dispatch outbound sintéticos

- Commit de implementação: `030be99`.
- Checkpoint sincronizado: `0dd18ac`.
- Tentativa `pending_dispatch` é persistida antes da rede; dispatcher usa lease/fencing, idempotency key externa estável e provider sintético fora da UoW.
- Sucesso cria PIX `pending` e leva a tentativa a `awaiting_confirmation`; timeout ambíguo exige reconciliação.
- Evidência fresca preservada em `.agent/verification.jsonl#VFY-CVG-002B2A-RECOVERY-001`: PostgreSQL 33/33, API 317/317 e worker 40/40.

## 3. Gate e autoridade B2b

O gate canônico já está operacional:

- Gate: [`.agent/gates/implementation-ready-CVG-002B2B.json`](../.agent/gates/implementation-ready-CVG-002B2B.json), registro `GATE-CVG-002B2B-IR-001`.
- Autoridade: `.agent/authority.jsonl`, `AUTH-CVG-002B2B-IR-001`.
- Escopo autorizado: código reversível, PostgreSQL descartável e provider local sintético para o slice inbound/shared-B1.
- Fora do escopo: provider real, credenciais, mutação externa, produção, deploy, release e aceitação de risco residual fora do slice.
- Fingerprint do gate: `94f5a269f82d8e0c64164dad2394c39df2f5c622672740cc5a22b072a40ae043`.

A revisão documental independente rejeitou duas versões intermediárias e aprovou somente a prontidão condicional da versão corrigida. O artefato completo está em `.agent/artifacts/CVG-002B2B-gate-review-2026-08-22.md`. Essa aprovação não é evidência de comportamento implementado: ACLs duráveis, UoW específica, principals, protocolo HTTP, migration e cobertura dedicada ainda precisam ser revalidados durante a implementação.

## 4. Trabalho executado nesta sessão

### RED e GREEN `B1-B2-001`

Foi escrito primeiro um teste de integração que reserva uma tentativa `pending_dispatch`, liga um PIX `pending` à tentativa e chama o mesmo comando B1. Antes da implementação, a suíte apresentou o RED esperado: 15 testes, 14 verdes e 1 falha em `PIX_TRANSACTION_NOT_COMPLETED`.

O menor GREEN foi aplicado no núcleo legado, ainda em `apps/api`:

- `ApplyConfirmedPixSettlementInput` agora aceita `attemptId` opcional e o comando valida o UUID.
- O repositório trava e valida a tentativa dentro da ordem billing → PIX → attempt.
- Para a extensão B2, o B1 aceita PIX `pending` ligado ao attempt, grava `completed_at`, faz `pending → confirmed_pending_apply → settled`, e o trigger libera `billing_records.active_payment_attempt_id`.
- O caminho sem `attemptId` preserva o comportamento B1 existente.
- A suíte focada terminou com 18/18 verdes após a revisão independente; isso comprova apenas esse incremento, não o B2b inteiro.
- O núcleo foi extraído para `packages/modules/pix/src/confirmed-settlement/`; os dois arquivos antigos da API agora são somente shims de reexportação.
- `pnpm --filter @cvg-his-v2/module-pix... build`, `pnpm --filter @cvg-his-v2/api lint`, os 8 testes do módulo PIX, a regressão PostgreSQL 18/18 e o teste de identidade dos shims passaram.

Suíte executada:

```bash
pnpm exec vitest run \
  tests/integration/database/confirmed-pix-settlement-command.test.ts \
  --config vitest.integration.config.ts --reporter=dot
```

O teste novo está em `tests/integration/database/confirmed-pix-settlement-command.test.ts`; a implementação agora está em `packages/modules/pix/src/confirmed-settlement/`, e `apps/api/src/commands/confirmed-pix-settlement.ts` e `apps/api/src/confirmed-pix-settlement-repository.ts` são shims.

A revisão independente inicialmente rejeitou o GREEN por permitir timestamp divergente em PIX já concluído, não validar provider/reserva do attempt, não vincular replay ao attempt e não cobrir os três checkpoints novos. Esses REDs foram adicionados e corrigidos; a suíte final passou 18/18. O registro está em `.agent/artifacts/CVG-002B2B-gate-review-2026-08-22.md` e nos eventos `EVT-0040`/`EVT-0041`.

### Controle de execução

- `EVT-0038`: RED aceito, sem migration/callback/worker.
- `EVT-0039`: GREEN inicial 15/15 aceito, seguido de revisão independente.
- `EVT-0040`: revisão rejeitou o primeiro GREEN por quatro lacunas de integridade.
- `EVT-0041`: correções e REDs adicionais passaram 18/18.
- `EVT-0042`: extração, build/typecheck e identidade dos shims passaram; a revisão externa final de VERIFIED ainda está pendente.
- `EVT-0043`: os REDs de raw-body/HMAC foram escritos; antes da implementação, os dois módulos ausentes falharam na resolução, sem mascarar o RED.
- `EVT-0044`: leitor raw-body + verificador HMAC passaram 24/24, incluindo bytes exatos, limites 65.536/65.537, chunked/length enganoso, abort, freshness inclusiva, headers duplicados, encoding, keyring por tenant e comparação 32/32 instrumentável; `pnpm --filter @cvg-his-v2/api lint` também passou.
- `EVT-0045`: após endurecer a rejeição de arrays de header críticos (inclusive array de um item), a mesma suíte passou 25/25 e o lint da API permaneceu verde.
- `EVT-0046`: o checkpoint de execução foi corrigido para o schema canônico (`type: CHECKPOINT`), o checker retornou `11 PASS`, `1 WARN` histórico e `0 FAIL`, os scans de segredos/dependências passaram, e a documentação foi publicada/sincronizada sem incluir o cache pré-existente do design system.
- O backlog moveu `CVG-002B2B` para `IN_PROGRESS`; não marcar como `DONE` ou `VERIFIED` antes de todas as evidências do contrato.

## Checkpoint atual para a próxima sessão — EVT-0051

O sub-slice inbound/parser agora está implementado, testado, commitado em `61cdef4` e enviado para `origin/agent/sync-v4-full-program`, pronto para ser retomado sem reconstruir contexto:

- Parser autenticado em `apps/api/src/pix-provider-webhook-payload.ts`: UTF-8 fatal/BOM, JSON estrito, chaves duplicadas, allowlist, UUIDs canônicos, valor/moeda/timestamp e binding da conta.
- Fingerprints em `apps/api/src/pix-provider-event-fingerprints.ts`: `SHA256("cvg.pix.raw-body.v1" || 0x00 || rawBody)` e `SHA256("cvg.pix.claims.v1" || 0x00 || canonicalClaimsJson)` com ordem de chaves congelada.
- `0111` em `packages/db/migrations/0111_pix_provider_event_ingress.sql` + schema Drizzle: receipt append-only e delivery separada, FK composta para attempt, unicidade tenant/provider/event, RLS/FORCE, checks de hash/estado/lease/agendamento e ACL condicional sem fallback para roles default ausentes.
- Repository em `apps/api/src/pix-provider-event-ingress-repository.ts`: claims são reparseadas do mesmo raw body, fingerprints não vêm do chamador, receipt+delivery commitam juntos, replay/conflict são tenant-scoped, target incompatível vira erro opaco e savepoints cobrem races/failpoints.
- Runtime ACL em reconciler, init shell e Helm foi reaplicado depois do CRUD amplo; o teste cria roles descartáveis, roda o reconciler duas vezes, cobre `SELECT/INSERT/UPDATE/DELETE/TRUNCATE` e limpa com `DROP OWNED`.

Evidência fresca deste checkpoint:

```text
unit-focused (raw/HMAC/parser/fingerprints/role contract): 77/77
ingress PostgreSQL + RLS/ACL/concurrency: 11/11
B1 settlement regression: 18/18
B2a request/dispatch regression: 33/33
API lint; DB lint/build; Prettier; shell syntax; git diff: PASS
independent review: APPROVE (sem CRITICAL/HIGH neste sub-slice)
```

Arquivos canônicos de retomada: `.agent/state.json` (`EVT-0051`), `.agent/backlog.json`, `.agent/tasks/CVG-002B2B.md`, `.agent/execution-log.jsonl`, `.agent/verification.jsonl#VFY-CVG-002B2B-PARSER-INGRESS-001` e `.gauntlet/state.md`/`progress.md`.

O próximo trabalho local autorizado é: RED de callback HTTP real por socket (ACK/rate-limit/framing), depois migration e filtros de principal de serviço não interativo, primitive shared sem `idempotency_requests`, claim/fence/backoff e consumer que chama somente o B1 extraído. O checkpoint não afirma que esses itens existem.

## 5. Próximo passo obrigatório

1. Escrever o RED da rota HTTP real por socket (framing/chunks/abort/headers/rate-limit/ACK) e integrá-la ao parser/verifier/repository já verificados.
2. Implementar migration/filtros de principal não interativo em todas as entradas de login, depois o primitive shared sem `idempotency_requests`, claim/fence/backoff e consumer B1.
3. Antes do gate `VERIFIED`, executar restart/concurrency multi-pool, coverage dedicada dos seams críticos, scans, regressões completas, fronteira legada `410` e checker canônico.

Não implementar provider real, segredo real, deploy ou produção nesta autorização.

## 6. Contrato B2b que ainda precisa ser cumprido

O contrato em [`.agent/tasks/CVG-002B2B.md`](../.agent/tasks/CVG-002B2B.md) já congela, entre outros pontos:

- raw bytes, HMAC-SHA-256, key binding por account, janela inclusiva e limite de 64 KiB antes de parse/tenant/DB;
- receipt append-only e delivery operacional separados, idempotência por tenant/evento e fingerprints canônicos;
- attempt B2a obrigatório, com PIX/correlação local podendo ainda faltar;
- service principal explícito, purpose-scoped e incapaz de login interativo, cache, refresh/session ou MFA;
- ACL mínima durável após migration, reconciler, init e reruns Helm;
- primitive compartilhada do worker, sem reaproveitar a idempotência genérica que pode replayar resposta antes do B1/CAS;
- ordem billing → PIX → attempt no B1 e fencing CAS por delivery/lease;
- retry, takeover, backoff, terminalização, redrive e rollback determinísticos;
- fronteira legada `410` para tentativas B2, sem alcançar o settlement antigo;
- cobertura dedicada dos seams excluídos pela configuração global atual.

Esses itens são requisitos de implementação e teste, não afirmações de que já existem no código.

## 7. Pesquisa e limites preservados

A pesquisa oficial usada no contrato continua registrada no histórico do handoff: raw-body/signature e webhooks da Stripe, processamento assíncrono da Adyen, `timingSafeEqual` do Node e constraints/`ON CONFLICT`/`SKIP LOCKED`/RLS do PostgreSQL. A documentação consultada do Pagar.me não fechou um contrato V5 completo de HMAC/freshness; por isso o B2b segue somente com `local-pix` sintético.

O objetivo amplo do ERP veterinário não está concluído. `CVG-002` permanece `IN_PROGRESS/PARTIAL`; B2c/SPA/E2E, paridade Vetus, WCAG, performance, operação alvo, provider real, deploy e go-live continuam em gates separados.

## 8. Atualização de continuidade — EVT-0052/EVT-0053 — fronteira HTTP do callback

**Data:** 22 de agosto de 2026, 21:42 BRT

**Branch:** `agent/sync-v4-full-program`
**Estado:** implementação local do próximo slice, ainda subordinada ao gate `CVG-002B2B`; não é um gate `VERIFIED`.

Esta seção é a referência mais recente para a próxima sessão. O trabalho anterior de parser, fingerprints, migration `0111`, repository, RLS e ACL continua preservado e verificado conforme `EVT-0051`/`VFY-CVG-002B2B-PARSER-INGRESS-001`.

### O que foi implementado neste slice

- `apps/api/src/routes/pix-provider-webhook-routes.ts` cria a capability explícita `POST /webhooks/pix/synthetic/v1`.
- A ordem da fronteira é rate limit por IP → leitura dos bytes crus → HMAC/key binding/freshness → parser estrito → persistência de receipt+delivery → ACK `202` opaco (`{"accepted":true}`), sem IDs internos.
- O limite de 64 KiB e os erros de stream são tratados antes de parse/tenant/DB; respostas cobertas incluem `400`, `401`, `413`, `429` e `503`.
- `server.ts` monta a rota somente quando `pixProviderWebhookSyntheticEnabled=true`, antes da autenticação bearer/API key/tenant; readiness falha fechado em processos production-like.
- `index.ts` só interpreta o keyring e instancia o repository quando a capability está explicitamente habilitada. `PIX_WEBHOOK_KEYRING_JSON` valida JSON estrito, UUID canônico, base64 canônico de pelo menos 32 bytes e rejeita propriedades extras.
- O verificador HMAC recebe `rawHeaders` e rejeita duplicação de headers críticos preservada pelo Node; a configuração foi propagada para `.env.v2.example`, Docker Compose e valores Helm com a capability desabilitada por padrão.

### Evidência executada

```text
shared-config build: PASS
shared-config tests: 32/32
API lint/typecheck: PASS
verifier + keyring focused tests: 35/35
startup secret tests: 6/6
HTTP integration (node:http, chunks, duplicate Content-Type, 401/400/413/429): 6/6
```

O teste HTTP usa servidor real e request real, inclusive body dividido em chunks e `rawHeaders`; ele ainda não é a cobertura completa de socket manual exigida pelo contrato. Após o endurecimento do allowlist do keyring, o refresh focado passou 35/35 (`VFY-CVG-002B2B-HTTP-BOUNDARY-002`).

### O que ainda não está concluído

- Falta o harness `node:net` com framing manual para chunked, `Content-Length` divergente, abort no meio do body e todas as combinações de headers duplicados.
- Falta provar a barreira de ACK com repository pendente e uma integração HTTP → PostgreSQL real verificando receipt/delivery após o `202`.
- O caminho OpenAPI da callback ainda não foi adicionado/validado.
- Conflitos/erros do repository ainda precisam ser convertidos para respostas opacas de fronteira (`404/409` sem vazamento de IDs); a decisão final de ignorar `Origin`/CORS para callbacks também precisa ser congelada.
- Migration e filtros de principal de serviço não interativo, UoW compartilhada, claim/fence/backoff e consumer B1 ainda não existem.
- Provider real, credenciais, produção, deploy, SPA/B2c, paridade Vetus e release continuam fora deste checkpoint.

### Retomada rápida

```bash
cd /home/ricardo/cvg-his-v4
git switch agent/sync-v4-full-program
git status --short
pnpm --filter @cvg-his-v2/shared-config build
pnpm --filter @cvg-his-v2/shared-config test
pnpm --filter @cvg-his-v2/api lint
pnpm exec vitest run \
  tests/unit/api/pix-provider-webhook-verifier.test.ts \
  tests/unit/api/pix-provider-webhook-keyring.test.ts \
  --config vitest.config.ts --reporter=dot
pnpm exec vitest run tests/integration/pix-provider-webhook-http.test.ts \
  --config vitest.integration.config.ts --reporter=dot
```

Não incluir nem reverter o arquivo user-owned `packages/design-system/tsconfig.vue.tsbuildinfo`.

## 9. Checkpoint atual — EVT-0055/EVT-0056/EVT-0057/EVT-0058/EVT-0059

**Data:** 22 de agosto de 2026, 22:02 BRT

O trabalho local posterior ao commit `be1cb47` foi fechado e publicado em `bbbf902`; a revisão independente encontrou dois desalinhamentos médios no OpenAPI, corrigidos e publicados em `705052b`:

- o harness `node:net` agora envia chunked real, testa `Content-Length` acima/abaixo do limite, abort no meio do corpo e duplicidade de headers críticos;
- a integração valida que bearer, API key, `X-Account-ID` e `Origin` de navegador não substituem a autenticação HMAC do provider;
- o ACK permanece pendente até `repository.persist()` resolver; conflitos `409` e falhas internas viram envelopes opacos sem códigos ou IDs internos;
- a rota não passa pelo CORS de browser e não é mais tratada como tenantless quando a capability está desabilitada;
- `PixProviderEventIngressRepository` virou interface canônica implementada pelo repository de banco;
- `/webhooks/pix/synthetic/v1` e os schemas de payload/ACK estão no OpenAPI, com regexes de key ID, timestamp e assinatura idênticos ao verifier e um envelope de erro com `correlationId` string limitado a 255 caracteres.

### Evidência fresca

```text
HTTP integration (real node:net + node:http): 13/13
verifier + keyring focused: 35/35
shared-config: 32/32
startup descriptors: 6/6
API lint/typecheck: PASS
OpenAPI: 334 paths, 40 tags, 385 schemas; structural validation PASS
git diff --check: PASS
```

O teste de `Content-Length` menor é deliberadamente documentado: o parser HTTP do Node encerra a requisição incompleta antes de entregar um corpo JSON à aplicação; o aceite é `400` sem persistência. Isso não é mascarado como erro de domínio.

### O que a próxima sessão deve fazer

1. Extrair/validar o helper de UoW compartilhado do worker e executar a query do principal sob a role worker read-only real.
2. Adicionar retry explícito para falhas transitórias de PostgreSQL/transporte, restart/takeover, redrive/DLQ e observabilidade sem segredo.
3. Implementar e provar a barreira legada `410`; depois conectar o slice ao fluxo clínico-financeiro/SPA.
4. Reexecutar as regressões B1 `18/18`, B2a `33/33`, ingress `11/11`, foco `77/77`, HTTP `13/13`, worker `47/47`, scans, cobertura dedicada e checker canônico antes de qualquer gate `VERIFIED`.

### Limites que permanecem explícitos

O checkpoint não certifica produção, provider real, credenciais, deploy, operação alvo, portal do tutor, SPA/B2c, paridade Vetus `11/11 + 3/3`, WCAG, performance, laboratório, internação completa ou o objetivo geral do ERP. A pesquisa atualizada de ezyVet, Shepherd, Digitail, Vetspire, Covetrus Ascend e Provet Cloud está registrada na seção 8 de [`2026-08-22-auditoria-integral-e-pesquisa-erp.md`](2026-08-22-auditoria-integral-e-pesquisa-erp.md), com força de evidência e critérios executáveis.

## 10. Checkpoint de continuidade — EVT-0060 a EVT-0065

**Data:** 22 de agosto de 2026, 22:38 BRT
**Artefato detalhado:** [`.agent/artifacts/CVG-002B2B-service-principal-worker-2026-08-22.md`](../.agent/artifacts/CVG-002B2B-service-principal-worker-2026-08-22.md)

O que foi feito desde `705052b`:

- prova HTTP→PostgreSQL em outra conexão: receipt e delivery só ficam visíveis depois do `202`, e failpoint após receipt faz rollback sem linha parcial (2/2);
- migration `0112`, schema Drizzle e RLS/FORCE do principal de serviço purpose-scoped (integração 5/5, schema 3/3);
- exclusão de service/inactive/non-interactive de login, cache, sessão, refresh, bearer sync e MFA (guard 7/7, users 13/13, auth 30/30);
- matriz de ACL durável: API com mutações sensíveis mínimas, worker somente leitura de identidade não secreta e mapping, revogação após grants amplos, `NOINHERIT` e rerun idempotente (unit 7/7, integração 1/1);
- claim/fence/backoff e consumer B1 default-off com capability sintética explícita; claim concorrente, ausência de principal, correlação ausente, ausência de uso de `idempotency_requests` e fence stale (unit 6/6, integração 3/3, worker 47/47).

Uma revisão encontrou e corrigiu o uso incompatível de `FOR UPDATE` na resolução do principal: a role worker permanece read-only. O consumer lê e revalida o mapping dentro da transação, enquanto o B1 mantém a ordem billing → PIX → attempt e é o único dono das mutações financeiras.

Limitações que devem permanecer visíveis: o consumer ainda usa um contexto transacional local a ser extraído para helper compartilhado; a query sob role worker real ainda precisa de teste dedicado; falhas PostgreSQL/transporte não allowlisted terminalizam; não há prova completa de restart/takeover/redrive/DLQ, barreira `410`, provider real, SPA/E2E, paridade Vetus, WCAG, operação alvo ou produção. O cache user-owned `packages/design-system/tsconfig.vue.tsbuildinfo` continua fora do escopo.

## 11. Checkpoint de continuidade — EVT-0068 a EVT-0071

**Data:** 22 de agosto de 2026, 23:12 BRT
**Implementação:** `46b84cb`
**Artefato detalhado:** [`.agent/artifacts/CVG-002B2B-worker-uow-legacy-410-2026-08-22.md`](../.agent/artifacts/CVG-002B2B-worker-uow-legacy-410-2026-08-22.md)

O incremento fechou, em escopo limitado, os gaps locais que estavam imediatamente antes do gate `VERIFIED`:

- helper shared `runInTenantTransactionContext` sem `idempotency_requests`, com rollback, client lifetime guard e `AsyncLocalStorage` canônicos; B1 e CAS `applied` usam a mesma conexão/UoW;
- retry explícito para códigos transitórios de PostgreSQL/transporte, mantendo divergência/erro desconhecido terminal e fail-closed;
- `redrive` interno, bounded e explicitamente auditado, somente a partir de `reconciliation_required`, com reset de tentativas e no-op após a transição;
- query real de principal executada sob role worker read-only, com `FORCE RLS`, colunas não secretas e rejeição de `password_hash`;
- fronteira legada `/payments/pix/intents/:id/confirm` autenticada por API key retorna `410 LEGACY_PIX_CONFIRMATION_DISABLED` antes do gateway/evento para qualquer `pix_transactions.payment_attempt_id` preenchido; OpenAPI alinhado;
- `PixTransactionRecord` passou a carregar o vínculo opcional de attempt para que a barreira não dependa do estado em memória do gateway.

### Evidência fresca deste checkpoint

```text
shared context: 3/3
worker package: 48/48
worker PostgreSQL: 5/5 (claim/takeover, retry, stale fence, redrive auditado, UoW canonical)
runtime ACL/RLS: 8/8 (7 unit + 1 integração, incluindo query sob role worker)
API route 410: 3/3
PIX repository unit: 5/5
OpenAPI: 335 paths / 386 schemas; validation estrutural PASS
API/worker/shared build, lint, typecheck, secret scan e git diff --check: PASS
```

O estado ainda é honesto e parcial. A prova de takeover usa lease expirado, mas ainda falta um cenário de processo morto/restart após cada checkpoint de B1, matriz completa multi-pool/multi-worker, DLQ/observabilidade operacional e integração HTTP→PostgreSQL específica da barreira legada. Provider real, credenciais, produção, deploy, SPA/E2E, paridade Vetus, WCAG e a jornada ERP completa continuam pendentes. O cache `packages/design-system/tsconfig.vue.tsbuildinfo` permanece fora do commit.

### Retomada da próxima sessão

```bash
cd /home/ricardo/cvg-his-v4
git switch agent/sync-v4-full-program
git status --short
python3 /home/ricardo/.codex/skills/engineering-framework/scripts/check_state.py "$PWD"
```

Próximo trabalho local: provar crash/restart e observabilidade/DLQ, ampliar a integração HTTP→PostgreSQL da fronteira `410`, reexecutar as regressões B1 `18/18`, B2a `33/33`, ingress `11/11`, foco `77/77`, HTTP `13/13` e o worker `48/48`, e só então reavaliar `VERIFIED`. Não marcar `CVG-002B2B` ou o ERP geral como concluído.

## 12. Checkpoint de continuidade — EVT-0072 a EVT-0076

**Data:** 22 de agosto de 2026, após os testes PostgreSQL da sessão
**Artefato detalhado:** [`.agent/artifacts/CVG-002B2B-recovery-dlq-legacy-http-2026-08-22.md`](../.agent/artifacts/CVG-002B2B-recovery-dlq-legacy-http-2026-08-22.md)

Este incremento foi publicado nos commits `fdb0995` (implementação/testes), `75bfa72` (documentação/ledgers) e `13dbd01` (reconciliação dos hashes publicados). O caminho de exaustão automática do worker deixou de ser silencioso: `claimNextWithPromotion` retorna a contagem de promoções para `reconciliation_required`, o consumer emite evento terminal seguro e as métricas agregadas não carregam labels de cardinalidade ilimitada ou dados de tenant/delivery.

### Evidência fresca

```text
worker package: 54/54
shared transaction context: 4/4
API payments route: 4/4
PostgreSQL worker fencing/restart: 6/6
PostgreSQL service principals/RLS: 5/5
HTTP→PostgreSQL legacy boundary: 3/3
API keys module: 10/10
OpenAPI: 335 paths / 386 schemas
secret scan, builds/typechecks/lint e git diff --check: PASS
```

A prova de recovery usa dois pools PostgreSQL independentes: A perde o pool depois do claim e antes de B1/CAS; B espera o lease, recebe novo token/lease version, rejeita a execução stale e aplica o B1 uma vez. Isto demonstra fencing/takeover e ausência de segunda receipt/idempotência, mas não substitui um `SIGKILL` de processo real nem uma matriz completa de falhas em cada escrita.

A barreira HTTP→PostgreSQL cria e observa o `pix_transactions.payment_attempt_id` em conexões distintas: owner recebe `410` sem gateway/outbox, conta estrangeira recebe `404` opaco e PIX direto sem vínculo conserva `200` com uma chamada ao gateway e um evento. O harness agora usa o `DatabaseApiKeyRepository` real e prova oito requests concorrentes com uma chave de baixo limite (dois `201`, seis `429`); o gap residual é a política multi-réplica e a projeção mínima da principal, não o lookup pré-contexto ou o formato JSONB local.

### Retomada obrigatória

```bash
cd /home/ricardo/cvg-his-v4
git switch agent/sync-v4-full-program
git status --short
python3 /home/ricardo/.codex/skills/engineering-framework/scripts/check_state.py "$PWD"
```

Próximo trabalho local: publicar a capability least-privilege de autenticação API-key pré-contexto já implementada, adicionar endpoint/runbook/alertas de DLQ, definir a política multi-réplica de rate limit e a projeção mínima da principal, repetir as regressões B1/B2a/ingress/HTTP e abrir B2c/SPA separadamente. O ERP geral, provider real, paridade Vetus, WCAG, produção e release continuam não concluídos.

## 13. Checkpoint de continuidade — EVT-0079 a EVT-0083

**Data:** 23 de agosto de 2026, após a validação PostgreSQL/HTTP da sessão

O gap de autenticação API-key foi implementado e publicado em `62db87e`, sem alterar produção. A migration `0113_api_key_auth_boundary.sql` cria a capability `cvg_api_key_auth` (`NOLOGIN`, `NOINHERIT`, `NOBYPASSRLS`), tenantiza `api_key_usage` e `api_key_rate_limits`, adiciona FKs compostas/índices/RLS e expõe somente `app.resolve_active_api_key(text,text)` por `SECURITY DEFINER` com `search_path` fixo. O resolver usa prefixo + hash completo, estado ativo/expiração e limite de dois candidatos para o serviço falhar fechado em ambiguidade. O mesmo boundary expõe o probe estreito de conta PIX necessário para rejeitar IDs cross-tenant antes do gateway.

O `DatabaseApiKeyRepository` agora aceita JSONB nativo do `pg` (array) e string JSON compatível, valida todos os campos obrigatórios e executa o lookup exato fora de contexto tenant; operações de uso/rate-limit continuam em `withTenantQuery`. O reconciler, bootstrap shell e Helm revogam API-key tables do worker, concedem CRUD somente à API e liberam os dois resolvers somente à API. O helper compartilhado das rotas extraídas aplica `checkRateLimit` antes de `updateLastUsed`, incluindo resposta `429` opaca com `resetAt`.

### Evidência executada

```text
API-key service: 13/13
API-key mapper: 3/3
auth-helpers (RED → GREEN): 2/2
runtime ACL/RLS PostgreSQL: 1/1
HTTP→PostgreSQL legacy/rate-limit boundary sem adapter: 4/4 (inclui concorrência 2×201/6×429)
shared-database build/lint e API build: PASS
RLS: 153/154 tabelas protegidas, uma exceção documentada
OpenAPI: 335 paths / 386 schemas
Helm static validation e secret scan: PASS
```

O teste HTTP foi executado após recompilar `@cvg-his-v2/module-api-keys`, `@cvg-his-v2/shared-database` e `@cvg-his-v2/api`; isso evita que aliases `dist` antigos mascarem o caminho-fonte corrigido. A matriz prova owner `410` antes de gateway/outbox, foreign account `404` sem chamada ao gateway, PIX direto `200` com um único gateway/outbox e oito requests concorrentes resultando em dois `201` e seis `429`. A integração ACL valida execução dos resolvers pela API, negação ao worker/PUBLIC, capability sem login/inherit/bypass e uso de `SET ROLE` para resolver uma chave fora de tenant context.

### Limites que permanecem explícitos

- A operação de consumo de rate limit é atômica no PostgreSQL e passou a prova concorrente local; ainda falta benchmark multi-réplica e política operacional de janela/clock/failover.
- A recovery continua sendo takeover por lease/pool encerrado, não um `SIGKILL` real em cada checkpoint B1; DLQ endpoint, runbook, alertas e dashboard operacional não existem.
- Provider real, credenciais, produção, deploy, SPA/B2c, paridade Vetus `11/11 + 3/3`, WCAG, performance e certificação operacional permanecem fora do checkpoint.
- `pnpm readiness:enterprise` continua WARN/FAIL conhecido (95/100, paridade Vetus não verificada); isso não é regressão da fatia API-key.
- O cache user-owned `packages/design-system/tsconfig.vue.tsbuildinfo` continua deliberadamente fora dos commits.

### Retomada da próxima sessão

```bash
cd /home/ricardo/cvg-his-v4
git switch agent/sync-v4-full-program
git status --short
pnpm --filter @cvg-his-v2/module-api-keys build
pnpm --filter @cvg-his-v2/shared-database build
DATABASE_URL_TEST=postgres://postgres:postgres@localhost:5433/cvg_his_v2_test_debug \
  pnpm exec vitest run tests/integration/pix-legacy-confirmation-http-postgres.test.ts \
  --config vitest.integration.config.ts --reporter=verbose
python3 /home/ricardo/.codex/skills/engineering-framework/scripts/check_state.py "$PWD"
```

Próximo gap local: adicionar DLQ/runbook/alertas, definir a política multi-réplica de rate limit e reduzir a projeção da principal autenticada; depois repetir as regressões e abordar B2c/SPA como gates separados. Não marcar `CVG-002B2B`, `CVG-002` ou o ERP geral como concluídos.

## 14. Ponto de entrada da próxima sessão — 23/08/2026

O resumo curto e mais recente para retomada está em
[`2026-08-23-checkpoint-continuacao.md`](2026-08-23-checkpoint-continuacao.md).
Ele consolida o inventário atualizado de `docs/` (1.447 arquivos), o hash do
manifesto desta execução, os resultados independentes da auditoria operacional
e a ordem de trabalho restante. Este handoff continua sendo o histórico técnico
detalhado; não é necessário reler as seções antigas antes de executar a
retomada mínima indicada no novo checkpoint.

## Incremento posterior — DLQ operacional de settlement PIX (23/08/2026)

Implementação publicada em `35f68fd`; correção de observabilidade multi-réplica
publicada em `1217882`.

O slice seguinte foi implementado localmente e está descrito em
[`CVG-002B2B-pix-settlement-dlq-2026-08-23.md`](../.agent/artifacts/CVG-002B2B-pix-settlement-dlq-2026-08-23.md):

- endpoints tenant-scoped de consulta sanitizada e redrive auditado;
- migration `0114` com capability `cvg_pix_dlq_operator` e função PostgreSQL
  `SECURITY DEFINER` atômica, sem conceder `UPDATE` direto à API;
- reconciler, bootstrap shell e Helm alinhados com a ACL API/worker;
- OpenAPI, alerta Prometheus, dashboard Grafana e runbook publicados. O alerta
  usa o gauge atual `worker_pix_provider_settlement_reconciliation_required`,
  atualizado por contagens PostgreSQL agregadas a cada 15s; o contador de
  promoções é apenas histórico. Cada worker recalcula o conjunto completo de
  contas, portanto alerta/painel usam `max(...)` entre réplicas para evitar
  dupla contagem.
- erros diretos `404/503` carregam `correlationId` e agora correspondem ao
  schema `ErrorResponse` documentado;
- evidência local route `4/4`, PostgreSQL/ACL `3/3` (backlog durável `1→0` após
  redrive), runtime grants `9/9`, worker `54/54`, alert alignment `5/5`,
  API/DB/worker build, OpenAPI `337/390`, Helm e parsers estruturais PASS.

Este é um incremento de recuperabilidade operacional, não um gate ERP final.
O estado canônico continua `CVG-002B2B IN_PROGRESS/PARTIAL`; SIGKILL/restart
real, rate-limit multi-réplica, principal mínima, provider, SPA, Vetus,
WCAG, target ops e release permanecem abertos.

## Registro de handoff final — 23/08/2026, 02:08 BRT

O checkpoint curto em [`2026-08-23-checkpoint-continuacao.md`](2026-08-23-checkpoint-continuacao.md)
é a primeira leitura recomendada na próxima sessão. A base remota deste handoff
é `d525acc` e o único dirty path permitido é o cache
`packages/design-system/tsconfig.vue.tsbuildinfo`.

O checker canônico foi executado e retornou 11 PASS, 1 WARN histórico de
ownership paralelo (`CVG-001`/`CVG-002`) e 0 FAIL. A próxima ação não é repetir
o DLQ já entregue: é definir a política de rate limit multi-réplica, estreitar
o principal autenticado, executar a matriz real de SIGKILL/restart e repetir as
regressões B1/B2a/ingress/HTTP. O ERP geral, paridade Vetus, WCAG, provider,
SPA, target operations e release continuam sem evidência suficiente.

## Registro de continuidade adicional — 23/08/2026, 03:13 BRT

Antes desta onda documental, `HEAD` e `origin/agent/sync-v4-full-program`
estavam alinhados em `4a5ead11e7809dfecd50b607df2e7dee99c2b3d3`; o checker
continuava em 11 PASS, 1 WARN histórico e 0 FAIL. O único dirty path é o cache
user-owned do design-system, fora do escopo. A onda de documentação foi
publicada depois em `a7bacda985e678970d562d0c0be9eb72c33b9124`.

A auditoria read-only do worker fechou a interpretação do próximo gap: o
takeover por pool já coberto em 6/6 valida lease/fence, mas não valida
`SIGKILL` real. O settlement consumer precisa de checkpoints injetáveis e de
um harness com processos independentes para provar os quatro pontos de morte
(`after_claim_commit`, `before_b1`, `after_b1_before_cas` e
`after_applied_cas`), readiness/metrics e aplicação canônica única. O artefato
com o contrato, riscos e arquivos prováveis é
`.agent/artifacts/CVG-002B2B-process-restart-and-erp-slices-2026-08-23.md`.

O benchmark também foi convertido em uma próxima jornada clínica não
concorrente com PIX: `internação -> handoff/permanência -> diária -> item
cobrável`, com idempotência por stay/período, proveniência, auditoria, cutoff
de alta, RLS/tenant e estados de UI explícitos. Isso é backlog futuro, não
evidência de que a jornada já foi implementada.

## Atualização de continuidade — SIGKILL/restart concluído (23/08/2026)

O contrato de checkpoints do settlement consumer e o harness de dois processos
foram implementados e executados contra PostgreSQL descartável com provider
`local-pix` sintético. A matriz parametrizada passou `4/4` em
`after_claim_commit`, `before_b1`, `after_b1_before_cas` e
`after_applied_cas`, com `SIGKILL` real em A, takeover/fence em B, PIDs
distintos e probes `/ready`/`/metrics` observados nos dois processos.

As consultas finais confirmaram exatamente uma receipt e estado canônico
`delivery=applied`, billing/attempt settled e PIX completed/applied. Nos três
casos anteriores ao CAS, a lease foi retomada com `attempts=2` e
`lease_version=2`; no caso pós-CAS, o sucessor retornou `idle`. O Quality Bar
fechado e os comandos reproduzíveis estão em
`.agent/artifacts/CVG-002B2B-sigkill-restart-quality-bar-2026-08-23.md`.

A revisão independente deixou explícito o limite naquele momento: ainda não
havia uma corrida em que A permanecesse vivo após B assumir; esse cenário foi
adicionado e passou no registro mais recente desta sessão. A matriz continua
sem contagem detalhada de journal, outbox e inbox. O canal de checkpoint agora é
fd 3 dedicado, e o fixture exige guard explícito de teste antes de habilitar
provider sintético.

O gate B2b continua `IN_PROGRESS/PARTIAL`: esta prova local não é homologação
de provider, não exercita Redis failover/clock-skew e não substitui SPA/B2c,
paridade Vetus, WCAG, target operations, cobertura dedicada ou release. A
próxima fatia de produto continua `internação -> handoff/permanência -> diária
-> item cobrável`.

Reconciliação de publicação: o código e testes estão no commit `855f53f` e a
reconciliação documental anterior está em `260096a`, ambos na branch
`origin/agent/sync-v4-full-program`; o cache user-owned do design-system ficou
fora dos commits. Para retomar, valide que `HEAD` e `origin` concordam na ponta
publicada atual e leia o checkpoint curto antes de iniciar o race stale-fence,
Redis failover/clock-skew ou a próxima jornada clínico-financeira.

## Handoff adicional — cutoff de alta e auditoria Redis (23/08/2026)

O checkpoint seguinte avançou duas lacunas locais sem promover o gate global:

- Redis local passou `21/21`, incluindo atomicidade, relógio Redis sob clock
  skew, deadline bounded e recuperação após cliente falho. Failover real entre
  processos continua aberto.
- A migration `0116_inpatient_discharge_cutoff.sql` fecha no PostgreSQL a
  fronteira de alta para `inpatient_progress`, `inpatient_occurrences`,
  `inpatient_daily_charges` e consumo de estoque
  `inpatient_stay`. O RED/GREEN descartável passou `2/2`.
- A rota HTTP de login agora tem prova `1/1` de fail-closed quando o rate
  limiter distribuído falha: `500 INTERNAL_ERROR` sem refresh cookie/token.

Artefato: [CVG-002D cutoff de alta](../.agent/artifacts/CVG-002D-inpatient-discharge-cutoff-2026-08-23.md).

O PostgreSQL compartilhado de testes entrou em recovery após repetidas bases
efêmeras; não iniciar novas rodadas nele sem que esteja saudável ou sem um
container descartável dedicado. A próxima implementação deve escrever o RED
de atomicidade/rollback entre billing item e marcação da diária e então
costurar admissão → handoff/permanência → estoque → alta → recebimento,
ledger, auditoria e outbox com RLS. Provider, SPA/B2c, paridade Vetus, WCAG,
target operations, cobertura e release continuam gates separados.

## Publicação do cutoff de alta

O implementation checkpoint `2b33aea` está publicado em
`origin/agent/sync-v4-full-program` e inclui a migration 0116, o teste de
cutoff, o artefato CVG-002D e os ledgers/hand-offs atualizados. O ponteiro
documental está reconciliado em `432887f`, com `HEAD == origin`. O cache
`packages/design-system/tsconfig.vue.tsbuildinfo` continua preservado, sujo e
fora dos commits. A próxima sessão pode escrever diretamente o RED de rollback
entre item de billing e marcação da diária.

## Handoff adicional — rollback diaria/billing (23/08/2026)

A proxima fatia de produto saiu do RED e entrou como implementacao limitada. A
rota de billing de diaria de internacao agora passa por `runTenantCommand`; a
criacao do item `daily_rate`, a marcacao da diaria, a persistencia e a auditoria
aguardada ficam no mesmo comando. Se houver falha, `BillingService` e
`InpatientService` reidratam os caches da conta com as linhas commitadas para
evitar estado fantasma no retry.

Evidencia local:

- API build PASS.
- `tests/integration/database/inpatient-daily-charge-billing-rollback.test.ts`:
  1/1 PASS em PostgreSQL efemero.
- `apps/api/dist/routes/inpatient-routes.test.js`: 12/12 PASS.
- `git diff --check`: PASS.

Artefato: [CVG-002C rollback diaria/billing](../.agent/artifacts/CVG-002C-inpatient-daily-billing-rollback-2026-08-23.md).

Ainda aberto: jornada completa de internacao ate recebimento/ledger/outbox, dois
tenants HTTP, concorrencia, SPA, Vetus, WCAG, providers, target operations,
cobertura global e release.

Publicação: `ee04d92e5ed50c0cf6c6f46fca4c53d17d1ba36c`
(`fix: make inpatient daily-charge billing atomic`) em
`origin/agent/sync-v4-full-program`; `HEAD == origin` foi confirmado após
`git fetch`. O cache `packages/design-system/tsconfig.vue.tsbuildinfo` segue
fora do commit.

### Revisão independente pós-publicação

Sem P0. A próxima sessão deve começar por uma prova HTTP com
`runTenantCommand` real e `Idempotency-Key` (P1), pois a integração atual prova
diretamente a UoW PostgreSQL. Também deve avaliar a invalidação/reidratação do
cache de auditoria em falhas tardias (P2), já que o evento pode entrar no cache
antes do rollback do banco. A jornada completa, replay/concurrency, RLS entre
tenants e os gates de produção continuam abertos.

## Handoff adicional — recibo de caixa HTTP/UoW (23/08/2026)

O P1 da revisão anterior foi fechado para a superfície pública de recibo. O
commit de implementação local é `3e278c8` (`fix: wire cash receipts through
tenant commands`) e o artefato detalhado está em
[`.agent/artifacts/CVG-002C2-cash-receipt-http-uow-2026-08-23.md`](../.agent/artifacts/CVG-002C2-cash-receipt-http-uow-2026-08-23.md).

- A rota `POST /encounters/:encounterId/cash-receipts` recebe o
  `runTenantCommand` real pelo servidor e encaminha a operação
  `encounter.cash-receipt.create`, a chave de idempotência e payload JSON-safe.
- O wrapper global de mutações HTTP continua dono da UoW/idempotência por
  operação HTTP; o runner da rota passa por uma transação já ativa sem
  aninhamento.
- O snapshot do response buffer omite `statusMessage` indefinido, corrigindo a
  rejeição do replay pelo canonicalizador JSON.
- A integração PostgreSQL efêmera autentica no endpoint publicado e prova
  primeiro POST, replay idempotente e conflito de payload. A consulta final
  confirmou exatamente um recibo, pagamento, movimento, journal, auditoria,
  outbox e idempotência concluída; a billing record termina `settled`.

Evidência: rota + buffer `10/10`, helpers HTTP `6/6`, comando PostgreSQL
`8/8`, HTTP/PostgreSQL publicado `1/1`, API typecheck PASS, `git diff --check`
PASS e crítica independente `APPROVE` sem P0/P1.

Limite remanescente: falta a matriz HTTP A/B com um segundo tenant/token para
provar explicitamente o bloqueio cross-tenant na superfície publicada. A RLS e
o repository já têm isolamento próprio. O programa continua
`IN_PROGRESS/PARTIAL`; a próxima sessão deve expandir admissão →
handoff/permanência → estoque → alta → billing → recebimento/ledger/auditoria/
outbox com replay, concorrência, dois tenants e failpoints. Provider, SPA/B2c,
paridade Vetus, WCAG, Redis failover real, operações alvo, cobertura e release
continuam abertos.
Publicação confirmada: `3e278c8` (implementação) e `d605351`
(`docs: record cash receipt HTTP checkpoint`) estão em
`origin/agent/sync-v4-full-program`; `HEAD == origin` foi verificado após
`git fetch`. O cache user-owned `packages/design-system/tsconfig.vue.tsbuildinfo`
segue fora do commit. A retomada começa pela matriz HTTP cross-tenant P2 e pela
jornada clínica-financeira completa, mantendo o programa `IN_PROGRESS/PARTIAL`.
## Handoff adicional — isolamento HTTP cross-tenant fechado (23/08/2026)

O P2 apontado pela crítica independente foi fechado no commit `0e0163c`
(`test: prove cash receipt tenant isolation`). A integração agora autentica um
segundo usuário/tenant e verifica a superfície publicada:

- GET de um recibo do tenant A com token B retorna `404
  CASH_RECEIPT_NOT_FOUND`;
- POST no encounter A com token B retorna `404 BILLING_RECORD_NOT_FOUND`;
- SQL confirma zero recibos e zero idempotência persistida no tenant B.

Resultado fresco: HTTP/PostgreSQL `2/2`, rota + buffer `10/10`, API typecheck e
diff check PASS. O slice de recibo de caixa não tem mais o P2 HTTP cross-tenant
pendente. A próxima sessão deve partir para a jornada clínica-financeira
admissão → handoff/permanência → estoque → alta → billing → recebimento/
ledger/auditoria/outbox, com replay, concorrência, failpoints e PostgreSQL/RLS;
produção, providers, SPA/B2c, paridade Vetus, WCAG, Redis failover real,
operações, cobertura e release continuam abertos.
Publicação confirmada: `0e0163c` (teste A/B) e `037053c` (checkpoint) estão em
`origin/agent/sync-v4-full-program`; `HEAD == origin` foi confirmado após
`git fetch`. O próximo ponto de entrada é a jornada clínica-financeira maior,
não há P2 HTTP cross-tenant pendente neste boundary.

## Handoff adicional — diária HTTP/UoW e recuperação de cache (23/08/2026)

A primeira prova HTTP real da cobrança diária de internação foi publicada em
`9a93ebc` (`fix: harden inpatient daily-charge HTTP billing`). O replay de uma
diária já faturada permanece dentro do `runCommand` tenant-aware. Em falhas,
quando o wrapper HTTP ainda mantém uma transação abortada, a reidratação dos
caches de internação e billing é agendada para depois do rollback; fora de uma
UoW externa, o caminho continua síncrono para os testes unitários.

Evidência fresca:

- API build PASS;
- rota compilada de internação `13/13`;
- module-inpatient `17/17`;
- module-billing `16/16`;
- integração HTTP/PostgreSQL `3/3`;
- `git diff --check` PASS.

A integração prova primeiro POST, replay com a mesma `Idempotency-Key`, conflito
de payload, rollback forçado entre item de billing e marcação da diária e duas
requisições concorrentes convergindo para um único item e uma única linha de
idempotência. O artefato detalhado é
`.agent/artifacts/CVG-002C3-inpatient-daily-charge-http-uow-2026-08-23.md`.

O programa continua `IN_PROGRESS/PARTIAL`. Faltam a matriz HTTP A/B da
internação, a inspeção do cache de auditoria em falha tardia e a jornada
admissão → handoff/permanência → estoque → alta → billing →
recebimento/ledger/auditoria/outbox. Redis failover entre processos, provider,
SPA/B2c, paridade Vetus, WCAG, operações alvo, cobertura e release permanecem
gates abertos. O cache user-owned
`packages/design-system/tsconfig.vue.tsbuildinfo` continua fora do commit.
Publicação documental final da diária HTTP/UoW: `46896bc4bf9990420c3ee9b651101c7c5376b6d4` está em `origin/agent/sync-v4-full-program`, com `HEAD == origin` confirmado. O checker canônico ficou em 11 PASS, 1 WARN histórico e 0 FAIL; somente o cache user-owned `packages/design-system/tsconfig.vue.tsbuildinfo` permanece dirty e fora do commit. Retomar pela matriz HTTP A/B da internação e pela revisão de auditoria em falha tardia.
Ponteiro final: `HEAD == origin/agent/sync-v4-full-program == c8d78d0`; a
verificação é `VFY-DOCS-CONTINUATION-019`. O próximo agente deve preservar o
cache `packages/design-system/tsconfig.vue.tsbuildinfo` fora do stage e iniciar
pela matriz HTTP A/B da internação.
Reconciliacao final do handoff: a sequencia publicada termina em `2f2760d`
(implementation `9a93ebc`, artifact/ledgers `46896bc`, pointers `c8d78d0` e
`2f2760d`), com `HEAD == origin` no momento da publicação. A verificação é
`VFY-DOCS-CONTINUATION-020`; retomar pela matriz HTTP A/B da internação.
