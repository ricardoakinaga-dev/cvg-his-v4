# Evidência terminal do CI exato — 2026-09-18

O candidato funcional avaliado foi
`4e71d3ff9f53a3b24e656ecb09d2f12311fb3a2d`; o HEAD documental publicado que
disparou a execução foi `52453612a7d45c1e2a002aae8e1e0a1f4eb7fcc9`.

O [CI #35304185255](https://github.com/ricardoakinaga-dev/cvg-his-v4/actions/runs/35304185255)
terminou `failure` em 2026-09-18. Os jobs `Build`, `Typecheck`, `Lint`, `SAST`,
`Secret Scan`, `Dependency Audit`, `Validate OpenAPI`, `Repository Guards`,
`Coverage`, `Unit Tests`, `Integration Tests`, `API Contract Tests`, `E2E Tests
(SPA)`, `Visual Regression` e `Critical Process Runner (Windows contract)`
terminaram `success`.

Os dois gates que falharam foram:

- `Critical Coverage Gate`, com exit code `1` no checker completo;
- `Performance (k6 SLOs)`, com exit codes `1` no benchmark e `99` na validação
  de SLO.

O artefato `performance-k6-report` foi publicado, mas a leitura REST do
download exige autenticação neste ambiente. Por isso, nenhuma métrica remota é
inferida a partir dos códigos de saída. O [State of Art Closure
#35304185156](https://github.com/ricardoakinaga-dev/cvg-his-v4/actions/runs/35304185156)
terminou `success`, mas não substitui os gates obrigatórios do CI.

Como diagnóstico independente, o mesmo perfil local `operational-minimum-v1`
(60 VUs, 3m30s, API compilada, PostgreSQL e Redis descartáveis) passou `9/9`
SLOs: API P95 `97,78 ms`, query P95 `107 ms`, erro `0%` e disponibilidade
`100%`. Essa evidência local não promove o CI remoto nem autoriza alterar
thresholds.

Conclusão: o candidato permanece **BLOCKED / NOT PROVEN**; não há claim de
`main green`, release ou `TRIPLE-A VERIFIED`.
