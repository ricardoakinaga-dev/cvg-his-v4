# CVG-001 — Ciclo seguro de instalação até sessão

## Contrato

- Status: `IN_PROGRESS`
- Estágio/atividade: `BUILD` / `IMPLEMENT`
- Responsável: root integrator
- Origem: barra ativa de 2026-08-07; sobreposição candidata de setup de 2026-08-10; `QB-SEC-01`, `QB-SEC-02`, `QB-DATA-01`, `QB-AUTH-01`, `QB-UX-01`, `QB-REL-01`
- Dependências: nenhuma externa para implementação local; PostgreSQL e Redis descartáveis para verificação
- Tier/risco/raio: `T4_CRITICAL` / `CRITICAL` / `CROSS_SYSTEM`

## Objetivo e resultado esperado

Em um banco vazio, criar exatamente uma instalação global, tenant, conta, unidade e administrador de modo atômico e auditado, usando segredo explícito que nunca aparece em log/resposta. O administrador deve autenticar, atualizar e revogar a sessão com autoridade compartilhada após reinício e entre duas instâncias simultâneas, sem vazamento entre tenants.

## Contexto confirmado e baseline histórico

Os pontos abaixo registram o baseline que motivou a tarefa, antes das correções
bounded já verificadas. Eles não representam o estado atual do código.

- O bootstrap candidato consultava `users` sob RLS; uma role de produção
  `NOBYPASSRLS` podia observar zero usuários e reabrir o setup.
- Quando não configurado, o token bruto podia ser gerado e impresso; o segredo
  não era consumido e excluir o último usuário reabria a instalação.
- A auditoria acontecia após commit; permissões persistidas podiam ficar
  incompletas; grants divergiam entre Compose e Helm.
- Sessões e usuários possuíam caches locais; uma réplica aquecida não conhecia
  o usuário/sessão criados por outra e a rotação de refresh não era CAS.
- O rate limiter Redis usava read-modify-write não atômico e abria conexão por
  operação.
- O baseline observado foi: hardening 54/54, suite API 276/277, PostgreSQL
  indisponível na primeira tentativa, OpenAPI PASS e readiness estrito FAIL.
- Instruções aplicáveis: `.agent/instruction-scope.md` e Quality Bar v1 em
  `.gauntlet/state.md`.

## Estado atual bounded — 2026-08-25

- O segredo explícito `SETUP_BOOTSTRAP_TOKEN` e o sentinel durável de instalação
  já existem e são exercitados por testes de startup/SQL; não há geração
  automática nem retorno do segredo.
- O caminho de setup usa a capability `cvg_installer` em uma transação explícita
  com `SET LOCAL ROLE`. As roles de login permanecem `NOINHERIT`, sem ampliar
  privilégios globais do runtime.
- O teste real
  `tests/integration/process/setup-installation-to-session.test.ts` inicia duas
  APIs contra o mesmo PostgreSQL descartável. Ele prova status inicial, setup
  único, propagação entre réplicas, login, sessão na réplica oposta, rotação,
  rejeição do refresh antigo, logout, revogação e `409` no segundo setup.
- O RED inicial foi preservado como diagnóstico: a primeira execução retornou
  `503` por falta de capability efetiva sob `NOINHERIT`; o GREEN corrigiu o
  boundary com `SET LOCAL ROLE`, mantendo o modelo de privilégio.
- Evidência fresca: unidade de setup 26/26, processo setup→sessão 1/1,
  regressão instalação/ACL 9/9, contratos CI/roles 15/15, cleanup 5/5,
  process manifest 8/8, typecheck global 70/70 e lint direcionado PASS.

Esta evidência é local e bounded. Ela não fecha Playwright/axe do wizard,
oversize/invalid input em todas as rotas, Redis/failover, target RLS/FORCE RLS,
restart físico, cobertura global, produção ou release.

### Complemento de acessibilidade — hints do wizard

- `DsInput` agora associa qualquer hint visível ao controle com
  `aria-describedby`, preservando a referência de erro quando há validação.
- A mesma regra foi coberta para `input`, `textarea` e `select`; o SetupPage
  verifica explicitamente as referências de token e senha.
- O revisor independente encontrou inicialmente uma dívida de cobertura
  (textarea/select e precedência de erro), que foi fechada antes do checkpoint.

### Complemento de entrada HTTP — matriz negativa

- Antes do POST válido, o processo real cobre JSON malformado, payload não
  objeto, token inválido, campos inválidos e corpo acima de
  `SETUP_MAX_BODY_BYTES`.
- As respostas são normalizadas (`400`/`401`/`413`), o token inválido não
  aparece no response/log e o sentinel permanece `setupRequired=true` após
  todas as rejeições.
- A revisão independente confirmou a ordem e os códigos contra
  `apps/api/src/routes/setup-routes.ts`; o único RED intermediário foi uma
  expectativa de teste detalhada demais para a mensagem pública genérica.

## Superfície de mudança

- Direta: setup/provisionamento/startup/OpenAPI da API; migração e grants PostgreSQL; repositórios de usuário/sessão e autenticação; rate limiter compartilhado; wizard/serviço/rotas da SPA; testes unitários, integração e E2E.
- Consumidores: `apps/api/src/index.ts`, `server.ts`, middleware de tenant/RBAC, `UsersService`, `AuthService`, Compose/Helm e clientes OpenAPI.
- Dados/config/operação: sentinel durável de instalação singleton; função de bootstrap com privilégio mínimo; `SETUP_BOOTSTRAP_TOKEN` como segredo explícito; auditoria no mesmo commit; PostgreSQL/Redis reais no harness.
- Limites invariáveis: nenhuma credencial em fonte/log/URL/control-plane; runtime normal sem CRUD global amplo; falha fechada; refresh somente em cookie HttpOnly/SameSite; default deny/RLS; nenhuma mutação de produção.

## Abordagem de implementação

1. Escrever testes RED para ausência/registro do segredo, setup singleton/RLS/concorrência/falha, permissões/auditoria, corpo inválido/oversize e resposta pós-commit.
2. Introduzir uma marca de instalação durável e uma função `SECURITY DEFINER` estreita, com `search_path` seguro, advisory lock, validação e auditoria na mesma transação. Conceder execução a uma role instaladora estável; não conceder CRUD global ao runtime.
3. Tratar cache de usuário/sessão como otimização: resolver misses no banco, sincronizar sessão por requisição e rotacionar refresh com compare-and-swap. Persistir/desenhar MFA compartilhado antes de declarar a parte correspondente completa.
4. Tornar incremento de limite atômico no Redis com conexão reutilizada. Separar buckets de status e criação.
5. Atualizar SPA/OpenAPI para estados indisponível/necessário/concluído e erros 400/401/409/413/503 sem retornar segredo.
6. Demonstrar o fluxo por HTTP/SPA com banco vazio, duas chamadas concorrentes, duas instâncias já ativas, reinício, replay, dois tenants e inspeção de estado.

## Contrato de verificação

| Critério | Procedimento/ambiente | Observação esperada | Superfície de regressão |
| --- | --- | --- | --- |
| `QB-SEC-01` | testes startup/API + captura/scans de logs | segredo ausente bloqueia mutação; valor nunca é gerado, retornado ou logado | config, setup, CI, OpenAPI |
| `QB-SEC-02` | HTTP e SQL reais, dois tenants e tentativas negativas | apenas ator/segredo/tenant autorizados; RLS e auditoria negam cruzamento | setup, auth, RBAC, tenant-context |
| `QB-DATA-01` | PostgreSQL vazio, concorrência e falha injetada | uma instalação completa; segunda perde; retry não duplica; zero estado parcial | migrations, schema, grants, audit |
| `QB-AUTH-01` | PostgreSQL/Redis, duas APIs quentes, reinício e replay | login/refresh/revogação/expiração autoritativos em qualquer instância | auth, sessions, users, rate limit |
| `QB-UX-01` | Playwright desktop/mobile, teclado e axe | estados de setup são claros, recuperáveis, sem segredo na UI/URL | SPA/router/service |
| `QB-REL-01` | unit/integration/E2E, typecheck/build/lint/coverage | gates verdes, sem skip/retry, cobertura significativa >=80% | monorepo e CI |

## Aceitação e DoD dinâmico

- [ ] Setup só muta estado quando explicitamente habilitado por segredo forte e nunca expõe o segredo.
- [ ] Estado singleton fecha instalação para sempre após sucesso; remoção do último usuário não reabre.
- [ ] Uma única transação cria o grafo e auditoria completos; corrida/falha/retry preservam invariantes.
- [ ] Permissões completas sobrevivem a reinício e grants de Compose/Helm seguem privilégio mínimo equivalente.
- [ ] Sessão compartilhada rejeita replay/revogação e funciona entre duas instâncias já ativas.
- [ ] Entradas inválidas/oversize e falhas pós-commit possuem semântica HTTP explícita e segura.
- [ ] O wizard é acessível e mostra configuração, progresso, erro e recuperação sem carregar segredo.
- [ ] Obrigações de implementação, regressão, segurança/dados, documentação, estado/evidência e risco residual da Quality Bar são satisfeitas.

## Resultado e evidência

- Arquivos/artefatos: `apps/api/src/setup-provisioning.ts`, testes unitários de
  provisionamento/rotas, `tests/integration/process/setup-installation-to-session.test.ts`,
  `infra/scripts/run-critical-process-suite.mjs`, o cleanup de fixtures e
  `packages/design-system/src/vue/DsInput.vue`.
- Procedimentos/resultados: RED/GREEN e regressões bounded executados; os
  resultados estão registrados em `.agent/verification.jsonl` como
  `VFY-CVG-001-SETUP-HTTP-001`, `VFY-CVG-001-SETUP-HTTP-REGRESSION-001` e
  `VFY-CVG-001-SETUP-HTTP-REVIEW-001`, além de
  `VFY-CVG-001-SETUP-A11Y-001`, `VFY-CVG-001-SETUP-A11Y-REGRESSION-001` e
  `VFY-CVG-001-SETUP-A11Y-REVIEW-001`, além de
  `VFY-CVG-001-SETUP-HTTP-INPUTS-001` e
  `VFY-CVG-001-SETUP-HTTP-INPUTS-REVIEW-001`.
- Riscos restantes: a tarefa permanece `IN_PROGRESS`; a prova de sessão não é
  certificação de UX, target environment, Redis, cobertura ou release.
- Próxima ação: executar a prova browser Playwright/axe do wizard quando o
  harness autorizado estiver disponível e fechar invalid/oversize input em uma
  matriz HTTP dedicada; depois repetir a jornada em ambiente autorizado e
  medir restart, restore/RTO-RPO e failover compartilhado.

## Complemento browser/axe — 2026-08-25

O wizard agora tem evidência contra o artefato SPA construído, pelo comando
oficial `pnpm test:e2e:spa:setup`: Chromium passou 4/4. A cobertura inclui
formulário nomeado, hints e erros associados por `aria-describedby`, teclado e
foco, `aria-invalid`, viewport de 390px sem overflow, axe com tags WCAG 2.1 e
2.2 AA, ausência de cookies e payload HTTP exato nos caminhos de sucesso e
retry.

O RED do axe encontrou contraste 3,96:1 no texto auxiliar claro; o token foi
corrigido para `#475569`, e o texto contextual recebeu cor opaca `#f8fafc`.
Falhas de conclusão agora limpam token, senha e confirmação; o teste confirma
que o retry é possível. SetupPage passou 8/8, DsInput 9/9, SPA typecheck/build
passou e a seleção explícita foi adicionada ao CI enterprise. A revisão
independente final de Kepler retornou `APPROVE_BOUNDED`, sem HIGH/MEDIUM.

O resultado continua limitado ao wizard: jornadas críticas mais amplas,
RLS/FORCE RLS do alvo, Redis/failover, restore/RTO-RPO, cobertura global,
produção, parity, operações e release permanecem abertos. CVG-001 continua
`IN_PROGRESS`.
