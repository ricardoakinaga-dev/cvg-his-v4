# REM-004/REM-005 — contratos de startup secrets e payload 413

timestamp_utc=2026-09-21T16:52:16+00:00
repository=/home/ricardo/cvg-his-v4
head=ad2f0373f68635f2579253b013a4382219906921
runtime=node-v22.23.2
package_manager=pnpm-10.33.0

## Contexto histórico verificável

- REM-004 já está implementado no candidato por `ea363b2990425c0d3b4aa0042cdf5216a95f4a70`: `METRICS_AUTH_TOKEN` foi adicionado ao mapa de segredos API (`production/metrics`) e ao teste de descritores.
- REM-005 já está implementado por `d8092d1728d54c79b8d3e746d3070dd7bf84020c`: `PayloadTooLargeError`/HTTP 413, limite declarado antes do consumo e testes de fronteira foram adicionados.
- Os arquivos de produto/teste dessas duas correções estão limpos em relação ao HEAD atual; este ciclo verificou o contrato existente em vez de duplicar mudanças.

## Comandos e resultados

1. `source /home/ricardo/.nvm/nvm.sh && nvm use 22.23.2 && pnpm --filter @cvg-his-v2/api build`: `exit 0`; TypeScript compilado e OpenAPI copiado.
2. `NODE_ENV=test node --test apps/api/dist/startup-secrets.test.js apps/api/dist/helpers/common.test.js`: `exit 0`.
   - 9 testes executados, 9 passaram, 0 falharam.
   - REM-004 cobre mapeamento `METRICS_AUTH_TOKEN`, resolução Vault, preservação de env explícito e fail-closed de configuração incompleta.
   - REM-005 cobre payload acima do limite por stream, `content-length` acima do limite, declaração não representável e fronteira de upload; todos exigem `PayloadTooLargeError` com status 413.
3. O teste usou somente fetch mock/synthetic Vault; nenhum segredo real ou provider externo foi acessado.

## Limites

Esta evidência valida os contratos focais no runtime oficial. Não prova ainda o gate agregado de coverage, a matriz completa de testes, o target, CI, OCI, UAT ou release. Os logs de teste emitiram somente caminhos sintéticos de Vault; valores de segredo não foram registrados.

result=PASS
