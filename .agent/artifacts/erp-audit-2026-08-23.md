# Auditoria integral e pesquisa — 2026-08-23

## Escopo

Checkpoint T4 de retomada para CVG HIS v4. A auditoria cobriu a árvore
documental atual, o plano/controle de execução, os baselines de readiness,
paridade, RLS, OpenAPI, consistência de migrações e o teste crítico PostgreSQL.
Nenhum ambiente de produção, provedor real ou credencial foi alterado.

## Corpus documental

Snapshot antes desta inclusão: 1.454 arquivos sob docs/, 1.198 textuais,
256 binários, 53.895.398 bytes, 360.826 linhas, 1.002 Markdown, 129 JSON,
67 HTML, 255 PNG e 1 gzip. O manifesto ordenado por caminho + NUL + conteúdo
em hexadecimal tem SHA-256
1e66d6af2cff706ccf2ac6291680b9fd1795c18ca0e71d84c7cbbcd3a8f290cd.

A precedência mantida é: runtime/testes/estado, código/contratos, camada ativa
de agosto, arquitetura/ADRs, auditorias de julho, Vetus e docs2 histórico.

## Resultados executados

| Comando | Resultado |
| --- | --- |
| pnpm readiness:enterprise | exit 1; 95/100 estrutural; 0/11 paridade geral |
| pnpm vetus:parity:audit | exit 0 report-only; 0/11 |
| pnpm vetus:clinical-parity | exit 1; 0/3 |
| pnpm validate:rls | exit 0; 154/155, uma exceção documentada |
| pnpm validate:openapi | exit 0; 337 paths, 40 tags, 390 schemas |
| node tools/migration-consistency-report.mjs | exit 1; manifesto docs/phase-9-migration-manifest.json ausente |
| pnpm test:critical | exit 1; 28 arquivos, 385/387 testes passaram |

O teste crítico deixou dois bloqueios concretos para o próximo RED/GREEN:

1. inpatient-daily-charge-bill-http-postgres.test.ts cria identificador
   textual stayday_<token> numa coluna uuid e falha com 22P02 antes da
   asserção de rollback.
2. installation-state.test.ts procura uma linha literal de REVOKE no template
   Helm, enquanto o template atual monta a revogação por SELECT format(...); a
   política least-privilege deve ser preservada e o teste deve inspecionar
   renderização ou contrato equivalente.

Os testes de integração também registram rate limiter in-memory; isso não é
prova de Redis compartilhado entre réplicas.

## Evidências de implementação preservadas

- worker-event-consumers-postgres.test.ts: 3/3; financial 15/15; event-bus
  23/23; handlers/gateway 17/17; cartão PostgreSQL 3/3.
- Vertical clínica-financeira sob role LOGIN NOSUPERUSER NOBYPASSRLS: 5/5;
  restart/replay controlado: 1/1.
- Process boundary do worker/ACL: 1/1 e grants 11/11; a prova de eventos de
  domínio em processo filho com takeover completo continua aberta.
- O último commit de documentação é 1c60a12; o último código do slice é
  67d47e2; o cache user-owned de design-system continua fora do stage.

## Decisão

CVG-002C6 permanece IN_PROGRESS/PARTIAL. O ERP, a Quality Bar, readiness,
produção, provedores, SPA, paridade, WCAG, operações e release não foram
promovidos. A próxima ação é corrigir os dois bloqueios do teste crítico,
reexecutar o conjunto completo e só depois continuar a prova de domínio em
processo filho/SIGKILL, failpoints e webhook HTTP retry/DLQ/fence.

## Reteste crítico pós-fix — 23/08/2026, 19:03 BRT

O contrato Helm recebeu a revogação explícita
`REVOKE cvg_installer FROM :"worker_user";` em
`infra/helm/cvg-his-v2/templates/postgres-runtime-role-configmap.yaml`.
O ajuste foi revisado independentemente, sem achados Critical/High; o único
residual é que a prova continua estrutural (não aplica o chart nem consulta
membership em um cluster Kubernetes). O código está no commit `6afd1d9`.

As regressões focadas da árvore atual passaram: daily-charge HTTP PostgreSQL
**4/4**, installation-state **8/8**, runtime-role-grants **11/11** e os três
arquivos de integridade/FK/PIX **68/68**. A execução integral atual
`REQUIRE_TEST_DB=1 pnpm exec vitest run tests/integration/database
tests/integration/setup tests/integration/foundational.test.ts --config
vitest.integration.config.ts --reporter=dot` terminou `exit 1`, **382/387**
testes em **23/28** arquivos. Falharam cinco asserções/fixtures e um teardown:

- o full run ainda materializou `stayday_<token>` na constraint do daily-charge,
  embora o arquivo atual e o teste isolado usem UUID; isso é uma divergência de
  reprodutibilidade/cache/paralelismo que precisa ser investigada, não deve ser
  mascarada;
- duas asserções de FK e a asserção de unicidade de usuário foram preemptadas
  por validações/NOT NULL legítimas no full run, embora passem isoladamente;
- o fixture PIX de backfill recebeu FK de conta ausente no full run, embora
  passe isoladamente;
- `production-like-runtime-bootstrap.test.ts` excedeu 30s no `afterAll`.

Portanto `pnpm test:critical` segue `FAIL/PARTIAL`; o resultado não promove
`QB-REL-01`, `QB-SEC-01` ou qualquer gate global. O próximo passo é reproduzir
o full run com isolamento/cache controlado e corrigir apenas a causa
determinística, mantendo as asserções de segurança e os testes isolados como
evidência bounded.
